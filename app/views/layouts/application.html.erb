<!DOCTYPE html>
<html>
  <head>
    <title>Flameo</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"/>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
      <nav class="navbar navbar-custom navbar-fixed-top">
        <div class="navbar-header">
            <a href="/" style="margin: 0; float: none; text-align:center"><img class="navbar-brand" src="../images/flameo.png"/></a>
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <% if session[:user_id] %>
                <li>
                    <a href="/users">Users</a>
                </li>
                <li>
                    <a href="/posts">Posts</a>
                </li>
                <% end %>
            </ul>
            <div class="navbar-header pull-right">
                <% if session[:user_id] %>
                <p class="navbar-text">
                  <a class="navbar-link"><%= "#{@user.username}" if @user %></a>
                </p>
                <p class="navbar-text">
                    <a href="/signout" class="navbar-link">Sign out</a>
                </p>
                <% else %>
                <p class="navbar-text">
                    <a href="/signup" class="navbar-link">Sign up</a>
                </p>
                <p class="navbar-text">
                    <a href="/signin" class="navbar-link">Sign in</a>
                </p>
                <% end %>
            </div>
        </div>
    </nav>
    <%= yield %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </body>
</html>
